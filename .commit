feat: centralize product data in static JSON file

- Generate 1000 products and store in src/data/products.json
- Update mock PIM to import from static JSON instead of generating at runtime
- Update Solr sync script to use same static JSON data
- Ensures consistent product data between PIM API and Solr index
