fix: extract product data from 3rdparty PIM wrapper in backend API

- Backend /api/backend/v1/product/[productId] now extracts the product
  from the 3rdparty response wrapper { source, product }
- Returns just the product object to the frontend as expected
