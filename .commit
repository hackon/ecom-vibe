feat: implement JWT-based authentication with multi-step registration

3rdparty Auth Mock (/api/3rdparty/auth):
- JWT-like token generation with access/refresh tokens
- User storage with seeded demo accounts
- Customer types: Private, Professional, Employee
- Actions: login, register, complete-profile, refresh, logout, me

Backend Auth API (/api/backend/v1/auth/*):
- /login - authenticate with email/password
- /register - create account (step 1)
- /complete-profile - save customer profile (step 2)
- /refresh - get new access token
- /logout - invalidate session
- /me - get current user

Frontend:
- AuthContext with login, register, completeProfile, logout, refreshAuth
- Token storage in localStorage with automatic refresh
- AuthDrawer component with slide-in panel
- Login form with demo credentials display
- Register form with password validation
- Multi-step profile form:
  - Private: firstName, lastName, address, phone
  - Professional: orgId, orgName, contactPerson, address, phone
  - Employee: employeeEmail, department
- Header with Sign In/Register buttons
- User dropdown menu when authenticated
- Profile completion prompt for new users
