refactor: remove Strapi and DotCMS integrations, use mockCms only

- Remove all Strapi and DotCMS files and directories (scripts, libraries, 3rdParty configs)
- Update sync script to remove Strapi phase
- Update package.json to remove strapi:init, strapi:test, sync:strapi scripts
- Update CMS API routes to import directly from mockCms
- Update CLAUDE.md documentation to remove Strapi/DotCMS references
- System now uses only mockCms for CMS data (simpler architecture)
