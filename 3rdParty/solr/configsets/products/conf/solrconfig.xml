<?xml version="1.0" encoding="UTF-8" ?>
<config>
  <luceneMatchVersion>9.4</luceneMatchVersion>

  <dataDir>${solr.data.dir:}</dataDir>

  <directoryFactory name="DirectoryFactory" class="${solr.directoryFactory:solr.NRTCachingDirectoryFactory}"/>

  <codecFactory class="solr.SchemaCodecFactory"/>

  <schemaFactory class="ClassicIndexSchemaFactory"/>

  <indexConfig>
    <lockType>${solr.lock.type:native}</lockType>
  </indexConfig>

  <updateHandler class="solr.DirectUpdateHandler2">
    <updateLog>
      <str name="dir">${solr.ulog.dir:}</str>
      <int name="numVersionBuckets">${solr.ulog.numVersionBuckets:65536}</int>
    </updateLog>
    <autoCommit>
      <maxTime>${solr.autoCommit.maxTime:15000}</maxTime>
      <openSearcher>false</openSearcher>
    </autoCommit>
    <autoSoftCommit>
      <maxTime>${solr.autoSoftCommit.maxTime:1000}</maxTime>
    </autoSoftCommit>
  </updateHandler>

  <query>
    <maxBooleanClauses>${solr.max.booleanClauses:1024}</maxBooleanClauses>
    <filterCache class="solr.CaffeineCache" size="512" initialSize="512" autowarmCount="0"/>
    <queryResultCache class="solr.CaffeineCache" size="512" initialSize="512" autowarmCount="0"/>
    <documentCache class="solr.CaffeineCache" size="512" initialSize="512" autowarmCount="0"/>
    <enableLazyFieldLoading>true</enableLazyFieldLoading>
    <queryResultWindowSize>20</queryResultWindowSize>
    <queryResultMaxDocsCached>200</queryResultMaxDocsCached>
  </query>

  <!-- Request Handlers -->

  <!-- Standard search handler -->
  <requestHandler name="/select" class="solr.SearchHandler">
    <lst name="defaults">
      <str name="echoParams">explicit</str>
      <int name="rows">10</int>
      <str name="df">text</str>
      <str name="wt">json</str>
    </lst>
  </requestHandler>

  <!-- Faceted search handler for e-commerce -->
  <requestHandler name="/browse" class="solr.SearchHandler">
    <lst name="defaults">
      <str name="echoParams">explicit</str>
      <str name="wt">json</str>
      <str name="df">text</str>
      <int name="rows">20</int>
      <!-- Enable faceting -->
      <str name="facet">true</str>
      <str name="facet.mincount">1</str>
      <str name="facet.field">category</str>
      <str name="facet.field">woodType</str>
      <str name="facet.field">brand</str>
      <str name="facet.field">material</str>
      <str name="facet.field">grade</str>
      <str name="facet.field">finish</str>
      <str name="facet.field">type</str>
      <!-- Price ranges -->
      <str name="facet.range">price</str>
      <str name="f.price.facet.range.start">0</str>
      <str name="f.price.facet.range.end">500</str>
      <str name="f.price.facet.range.gap">50</str>
      <!-- Highlighting -->
      <str name="hl">true</str>
      <str name="hl.fl">name,description</str>
      <str name="hl.simple.pre">&lt;em&gt;</str>
      <str name="hl.simple.post">&lt;/em&gt;</str>
    </lst>
  </requestHandler>

  <!-- Autocomplete/suggest handler -->
  <requestHandler name="/suggest" class="solr.SearchHandler">
    <lst name="defaults">
      <str name="echoParams">explicit</str>
      <str name="wt">json</str>
      <str name="defType">edismax</str>
      <str name="qf">name_autocomplete^10 name^5 sku^3</str>
      <int name="rows">10</int>
      <str name="fl">id,name,category,price</str>
    </lst>
  </requestHandler>

  <!-- Update handler -->
  <requestHandler name="/update" class="solr.UpdateRequestHandler"/>

  <!-- Admin handlers -->
  <requestHandler name="/admin/ping" class="solr.PingRequestHandler">
    <lst name="invariants">
      <str name="q">*:*</str>
    </lst>
    <lst name="defaults">
      <str name="echoParams">all</str>
    </lst>
  </requestHandler>

  <requestHandler name="/analysis/field" startup="lazy" class="solr.FieldAnalysisRequestHandler"/>
  <requestHandler name="/analysis/document" startup="lazy" class="solr.DocumentAnalysisRequestHandler"/>

  <!-- Query parsers -->
  <queryParser name="edismax" class="solr.ExtendedDismaxQParserPlugin"/>
</config>
